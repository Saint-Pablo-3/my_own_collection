# Ansible Collection - my_own_namespace.yandex_cloud_elk

Collection containing a custom module `file_creator` and a single-role `file_creator`.

## Module

- `file_creator`: creates a file on a remote host with specified path and content.

## Role

- `file_creator`: role wrapping `file_creator` module with default parameters.

